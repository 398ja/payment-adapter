# Sample configuration for payment-gateway-client

# Base URL of the REST API (scheme + host; port optional)
# If no port is present, the gateway.api.port value (or default 8080) is applied.
gateway.api.base_url=http://localhost

# Port to use when base_url omits an explicit port
gateway.api.port=8080

